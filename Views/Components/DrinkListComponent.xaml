<UserControl
    x:Class="WinUIApp.Views.Components.DrinkListComponent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIApp.Views.Components"
    xmlns:models="using:WinUIApp.Models">

    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled" Padding="0 0 0 15">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Spacing="20">
            <ItemsRepeater x:Name="DrinksRepeater" ItemsSource="{x:Bind ViewModel.PersonalDrinks, Mode=OneWay}" Margin="0 0 0 10">
                <ItemsRepeater.Layout>
                    <StackLayout Orientation="Horizontal" Spacing="20"/>
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="models:Drink">
                        <Border Background="White"
                                CornerRadius="10"
                                Padding="15"
                                BorderBrush="Yellow"
                                BorderThickness="2"
                                Width="200"
                                Height="250">
                            <StackPanel Spacing="10" Width="200">
                                <Border Background="Gray"
                                        Height="150"
                                        CornerRadius="5">
                                    <Image Source="{x:Bind DrinkURL}"
                                           Stretch="UniformToFill"
                                           Width="200"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="{x:Bind DrinkName}"
                                           FontSize="18"
                                           FontWeight="Bold"
                                           Foreground="Black"
                                           TextWrapping="Wrap"/>
                                <TextBlock Text="{x:Bind Brand.Name}"
                                           FontSize="14"
                                           Foreground="Black"
                                           TextWrapping="Wrap"/>
                                <TextBlock Text="{x:Bind AlcoholContent}"
                                           FontSize="12"
                                           Foreground="Black"
                                           TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </StackPanel>
    </ScrollViewer>

</UserControl>