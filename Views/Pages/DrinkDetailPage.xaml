<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUIApp.Views.Pages.DrinkDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIApp.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <TextBlock HorizontalAlignment="Center"
                   x:Name="Name" 
                   FontSize="18" 
                   FontWeight="Bold"/>
        <Grid HorizontalAlignment="Center">
            <Image x:Name="DrinkImage"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Bottom"
                   Margin="90 0 0 0"/>
            <StackPanel Orientation="Vertical"
                        HorizontalAlignment="Right"
                        Margin="0 0 90 0">
            </StackPanel>
        </Grid>
        <StackPanel HorizontalAlignment="Center">
            <TextBlock Text="Brand" FontSize="18" FontWeight="Bold"/>
            <TextBlock x:Name="BrandText" FontSize="14"/>
            <TextBlock Text="Alcohol percentage" FontSize="18" FontWeight="Bold"/>
            <TextBlock x:Name="AlcoholPercentageText" FontSize="14"/>
            <TextBlock Text="Categories" FontSize="18" FontWeight="Bold"/>
            <TextBlock x:Name="CategoriesText" FontSize="14"/>
            <TextBlock x:Name="ReviewScoreText" FontSize="18" FontWeight="Bold"/>
        </StackPanel>
    </Grid>
</Page>