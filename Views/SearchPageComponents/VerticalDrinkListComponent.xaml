<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WinUIApp.Views.Components.VerticalDrinkListComponent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIApp.Views.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:local1="using:WinUIApp.Models"
    mc:Ignorable="d">

    <Grid>
        <ListView 
            x:Name="DrinkListView"
            ItemsSource="{x:Bind DrinksList}" 
            SelectionMode="None" 
            IsItemClickEnabled="True" 
            ItemClick="DrinkList_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local1:Drink">
                    <StackPanel Orientation="Vertical" Padding="10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Brand: " FontWeight="Bold" FontSize="16"/>
                            <TextBlock Text="{x:Bind Brand.Name}" FontWeight="Bold" FontSize="16"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Alcohol Content: " FontWeight="Bold" FontSize="16"/>
                            <TextBlock Text="{x:Bind AlcoholContent}" FontWeight="Bold" FontSize="16"/>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>